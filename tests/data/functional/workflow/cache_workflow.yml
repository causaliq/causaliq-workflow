
matrix:
  dataset: ["asia", "cancer"]
  algorithm: ["pc", "ges"]

steps:
  - name: "Cache Capture Step"
    uses: "cache_capturing_action"
    with:
      action: "capture"
      data: "{{dataset}}"
      algo: "{{algorithm}}"
