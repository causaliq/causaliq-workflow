# Example workflow using the built-in echo action
# Run with: cqflow run echo_workflow.yml --mode=run

description: "Test workflow using built-in echo action"
id: "echo-test"

steps:
  - name: "Echo Test"
    uses: "causaliq-workflow"
    with:
      action: "echo"
      message: "Hello from the echo action!"
      nodes: 4
